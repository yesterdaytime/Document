git 流程
	git status
	git diff
	git add rm
	git commit
	git push origin master
git 冲突解决：
	git pull origin master












git中遇到的问题	
    1、chang-id问题：
			gitdir=$(git rev-parse --git-dir); scp -p -P 29418 mingpeiw@tempest-reviews.eng.vmware.com:hooks/commit-msg ${gitdir}/hooks/
